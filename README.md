# Тестовое задание "Тысячи курсов"

Проект представляет собой мобильное приложение для каталога курсов, разработанное в рамках тестового задания. Приложение позволяет пользователю войти в систему (с локальной валидацией), просматривать список курсов, загружаемый из сети, и добавлять их в локальную базу данных "Избранное".

## 📱 Скриншоты

| Экран входа | Главный экран (Курсы) | Экран "Избранное" |
| :---: | :---: | :---: |
| <img width="1080" height="2400" alt="image" src="https://github.com/user-attachments/assets/f9f5606f-91f3-4ba1-b1e6-7aa1b113fa56" /> |  <img width="1080" height="2400" alt="image" src="https://github.com/user-attachments/assets/a9c2c337-1dac-4a87-b072-430e439ee2e1" /> | <img width="1080" height="2400" alt="image" src="https://github.com/user-attachments/assets/cab7bf40-9977-4992-80c4-3e98510f1bbb" /> |  <img width="1080" height="2400" alt="image" src="https://github.com/user-attachments/assets/2be7c19a-cb02-4d2e-8052-8a64664b2333" />

## 📋 Ключевые требования ТЗ

* [cite_start]**Архитектура:** Clean Architecture [cite: 30][cite_start], MVVM [cite: 27][cite_start], Многомодульность (обязательно)[cite: 31].
* [cite_start]**Стек:** Kotlin [cite: 22][cite_start], Retrofit (обязательно) [cite: 23][cite_start], Koin/Dagger [cite: 26][cite_start], Coroutines[cite: 24].
* **Функционал:**
    * [cite_start]**Вход:** Локальная валидация email и пароля, без запроса на сервер[cite: 51].
    * [cite_start]**Главный экран:** Загрузка списка курсов из JSON API[cite: 63, 65].
    * [cite_start]**Сортировка:** Сортировка списка по дате публикации[cite: 76].
    * [cite_start]**Избранное:** Сохранение курсов в локальную БД (Room)[cite: 97].
    * [cite_start]**Навигация:** Нижнее меню (Главная, Избранное, Аккаунт)[cite: 55].
    * [cite_start]**Аккаунт:** Экран-заглушка[cite: 57].
  
---

## 🏗 Архитектура

Проект построен на **многомодульной Clean Architecture** (Feature-based) для строгого разделения ответственности, переиспользования кода и упрощения масштабирования.



### 1. Модульная структура

* **:app**
    * Главный модуль приложения (Single Activity).
    * Отвечает за сборку DI-графа (Koin) и навигацию (`NavHost`).
    * Содержит `Application` класс.

* **:core** (Фундаментальные модули)
    * `:core:ui`: Общая дизайн-система (тема `AppTheme`, цвета, типографика, общие ресурсы и иконки).
    * `:core:network`: Настройка `Retrofit` и `OkHttp`, DI-модуль для сети.
    * `:core:database`: Настройка `Room`, `FavoritesDao` и DI-модуль для базы данных.

* **:features** (Функциональные модули)
    * Каждый модуль содержит 3 слоя Clean Architecture (`domain`, `data`, `presentation`).
    * `:features:auth`: Экран входа и логика валидации.
    * `:features:home`: Главный экран, список курсов, логика загрузки и сортировки, интеграция с БД (для статуса "избранное").
    * `:features:favorites`: Экран "Избранное", отображение списка из БД, логика удаления.
    * `:features:profile`: Экран-заглушка.

### 2. Поток данных (Clean Architecture)
Поток данных внутри каждой фичи строго однонаправленный (UDF):

`UI (Compose Screen)` → `ViewModel` → `Use Case (Domain)` → `Repository (Domain)` → `RepositoryImpl (Data)` → `(API / DAO)`

---

## 🛠 Технологический стек

* **UI:** Jetpack Compose, Material 3
* **Архитектура:** Clean Architecture, MVVM, Multi-module
* **Асинхронность:** Kotlin Coroutines, Flow
* **DI (Внедрение зависимостей):** Koin
* **Сеть:** Retrofit, OkHttp, Gson
* **База данных:** Room
* **Навигация:** Navigation Compose (Single Activity)
* **Загрузка изображений:** Coil
* **Сборка:** Gradle с Kotlin DSL и Version Catalog (`libs.versions.toml`)
